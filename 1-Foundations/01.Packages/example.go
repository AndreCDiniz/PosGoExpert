/* package example nesse caso teremos um erro pois por estar no mesmo pacote que a math.go e dentro dela ja existir o package main essa classe tambem deve ter o package main */

package main

const a = "André"
