/* Em relaçao a pacotes no Go, é sempre necessário que exista um "package main" e sempre os demais arquivos dentro da mesma pasta devem conter o mesmo nome do pacote nesse exemplo abaixo todos as classes dentro do folder Packages devem ter o main*/
/* Para rodar o programa nos usamos o go run no terminal porem para rodarmos tudo que esta dentro de um pacote usamos por exemplo "go run Packages/*" */

package main

func main() {
	println("Hello World " + a)
}
